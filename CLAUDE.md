# 프로젝트 가이드

개인 블로그 - 간단한 메모 형태로 정리

---

## 메모 작성 원칙 (필수)

**메모 ≠ 블로그 글**

**기본형: 키워드 + 간단한 코드 + 설명.** 이 크기를 넘으면 쪼개거나 덜어낼지 의심한다. 단 **나중에 실행할 일이 있는 메모는 쿡북**이 기본값이고 크기가 다르다 (아래 "쿡북" 절).

```
1. 한 문장으로 요약할 수 있는가?
2. 코드가 핵심이면 코드만
3. 링크가 핵심이면 링크 + 한 줄 설명
4. 헤딩(H1, H2...)은 정말 필요할 때만
5. "나중에 이해할 수 있는 최소한"만 작성
```

### 무엇을 담을 것인가 (선택 기준)

- 메모는 **망라가 아니라 "나에게 신선/인상 깊었던 것"**. 그 한 가지를 주인공으로 삼는다.
- **남의 경험담·일반 세부는 배제** — 원문 저자의 디버깅 사례 같은 건 내 인사이트가 아니다.
- 공개용이면 **개인 종속(내 라이브러리·작업 맥락)도 제거**.
- ⚠ AI로 정리하면 *일반적으로 인상적인 디테일*로 흐른다 (구체 숫자·보안 사례 등). 신선함은 사람이 직접 짚어야 한다.

### 압축 (필수)

**남의 문장을 그대로 옮긴 것은 메모가 아니다.** 한 줄로 줄여지지 않으면 아직 이해하지 못한 것.

- **인용할 거면 인용 표시를 한다** — `>` 또는 `QuoteLink`. 출처를 밝힌 인용은 정직하고, 표시 없는 번역문이 문제다.
- **확인할 수 없는 이름을 권위처럼 쓰지 않는다.** 정리 이름·개념어만 적어두면 근거처럼 보이지만 6개월 뒤 검증할 수단이 없다. 555·570·572·573·575에 붙어 있던 "형식 닻"(`ui_exclusive` 등)이 그랬다 — 도입 커밋이 에이전트 co-author였고, 저장소에 `.lean` 파일도 없고 웹에서도 안 나왔다. 남길 거면 **따라갈 수 있는 출처를 함께** 단다.
- **1인칭을 확인한다.** 원문을 옮기면 "제가 만든 이유"처럼 남의 1인칭이 따라 들어온다 (268.md가 그랬다).
- **걷어내면 형태가 드러난다.** 부풀린 산문을 지우면 남는 게 링크뿐일 수 있다 → `type`이 바뀐다 (268.md: `note` → `bookmarks`).
- 같은 내용이 두 단락에 반복되면 기계 요약의 흔적이다 (272.md).
- **코드가 보여주는 것을 산문으로 다시 쓰지 않는다.** `snippet`에서 산문이 코드를 번역하고 있으면 그 산문은 잉여다 (576.md는 첫 단락이 `배열 → union → 함수` 파생을 설명했는데 바로 아래 코드가 이미 그것이었다).
- **짧음과 즉답 가능성은 다르다.** 눌러 담으면 짧아지지만 나중에 재구성해야 하는 문장이 된다 — 압축의 기준은 글자 수가 아니라 "6개월 뒤 바로 읽히는가"다. 같은 주장이 산문·코드 주석·컴포넌트 속성에 흩어져 반복되는 것도 이때 생긴다 (576.md는 세 번 반복됐다).
- **기호는 단순화가 아니라 암호화가 될 수 있다.** 수식·연산자로 줄이면 짧아지지만, 그게 무엇을 줄인 것인지 본문이 말하지 않으면 6개월 뒤엔 해독 대상이 된다. **평범한 말이 먼저, 기호는 그걸 줄인 손잡이로 뒤에** — 573.md는 `τ`·`D`를 그렇게 쓰는데 575.md는 `render ∘ query ∘ defer`로 시작해 풀이가 없었고, 572.md는 `tSendDone`·`tResp`를 한 번도 풀지 않았다. 검산: **초등학생도 이해할 수 있게 써본다.** 길어지지만 읽힌다.
- **걷어낼 땐 그것에 기대고 있던 것도 함께 본다.** 프레임을 지우면 잔여물이 남는다 — 575.md에서 합성을 풀자 그 좌변이던 `combobox`와 `∘` 개수를 센 `세 단계`가 근거를 잃었고, 572.md의 `Reached = Processed`는 제거된 형식 닻과 짝이던 표현이었다. **산문 밖도 본다**: SVG의 `alt`, `AutoIframe`의 `title`, 코드 주석에 옛 용어가 그대로 남는다.

### 쿡북 — 행동을 담는 메모의 기본값

**갈림길은 "이 메모를 나중에 실행할 일이 있는가"다.** 있으면 쿡북, 없으면 맨 위의 기본형(키워드 + 간단한 코드 + 설명)이다. `bookmarks`·breadcrumb·`Explainer` 는 "쿡북에 안 맞는 것"이 아니라 **실행할 일이 없는 쪽**이다 — 링크 목록을 레시피로 만들면 `268` 에서 걷어냈던 부풀린 산문이 다시 들어온다.

**최종 모델이 아니라 기본값이다.** *다르게 할 이유가 없으면 이걸로* 라는 뜻이지 *다른 게 있으면 그게 틀렸다* 가 아니다. 차이는 안 맞는 걸 만났을 때 갈린다 — 기본값은 이유를 적고 넘어가고, 최종 모델은 **대상을 고치려 든다.** `305` 를 부풀리려 했던 게 후자다.

**쿡북은 크다.** `578` 을 옮기니 1,177 → 1,360자로 코퍼스 90퍼센타일(1,105)을 넘었다. 중앙값은 467자다 — 다섯 칸이 그 안에 안 들어간다. 기본형과 크기가 다른 게 정상이고, **둘이 크기로 서로를 판정하지 않는다.**

> [!NOTE]
> 표본 일곱: `578`·`576`·`575`·`574`·`573`·`570` = +16%, +9%, +8%, +5%, +12%, +8%. `572` 는 **적용하지 않았다**(처방이 없어 레시피가 아니다) — 일곱 중 하나가 "아니오". 전부 원래 큰 축이라 중앙값 467자짜리는 아직 모른다.

**검산은 "따라 하면 돌아가는가"다.** "6개월 뒤 읽히는가"만으로는 안 잡히는 게 있다 — `305.md`는 `kind: Recipe`에 `sources` 4개·`generated`·`verified`가 다 있고 `lint:memo`도 통과하는데, 본문이 "1. 서비스 계정 설정 2. 프로젝트 생성 …" 목차 7줄이라 **실행할 게 없다.** `243.md`를 고친 것도 문체가 아니라 `document.fonts.add()` 한 줄이었다.

**실행 가능 ≠ 코드.** 판별 절차·점검 목록·질문 세트도 실행된다. **레시피 안에서** 실행할 게 없는 건 목차뿐이다 — 애초에 실행할 일이 없는 메모는 위 갈림길에서 이미 갈렸다.

**순서가 내용인 메모도 있다 (breadcrumb).** `305`·`97` 은 GCP·앱스 스크립트를 **어떤 순서로 밟는지**만 남긴 것이다 — 한 번 해본 사람이 6개월 뒤 다시 올 때 필요한 게 차례뿐이고, 각 단계의 방법은 링크에 있다. 목차처럼 보이지만 목차가 아니다: 검산이 **"이 순서로 가면 막히지 않는가"**다. **부풀려 고치려 들면 안 된다** — 채워 넣는 순간 "빠르게 훑는다"는 원래 가치가 사라진다. 실제로 그럴 뻔했다.

**메모 하나 = 레시피 하나.** 쿡북은 레시피의 목록이지 한 편의 글이 아니다. 위에서 아래로 읽는 게 아니라 필요한 것만 꺼내 쓴다 — 레시피가 열 개면 메모도 열 개다. 여러 개가 묶이면 **"어떤 상황이면 어디로"를 가리키는 라우터 레시피**를 하나 두고 `parent`로 묶는다.

레시피의 뼈대:

| 칸 | 담는 것 |
|---|---|
| **언제** | 이 레시피를 꺼내는 상황. **이게 색인이다** — 없으면 목록이 아니라 문서가 된다. 있어도 **맨 아래 있으면 색인 노릇을 못 한다** (`574` 의 판별 기준이 결론 자리에 있었다) |
| 재료 | 전제·의존 (없으면 생략) |
| **절차** | 번호 붙은 실행 단계. 붙여넣어 돌아가거나, 따라가면 답이 나오거나 |
| **확인** | 됐는지 아는 방법. 없으면 절차가 맞았는지 알 수 없다 |
| 함정 | **실제로 밟은 것만.** 일반적 주의사항은 뺀다 |

**역검사: `확인` 칸이 안 채워지면 레시피가 아니다.** `확인` 은 *내가 한 일이 됐는가* 인데 한 일이 없으면 확인할 것도 없다. `572` 가 그랬다 — `언제` 도 되고 `절차` 1번(취소 시점에 전송이 끝났는지 본다)도 되는데 **2번이 없다.** "끝났으면 서버는 받았다"는 결론이지 행동이 아니다. 처방이 한 줄도 없으면 `Explainer` 로 두고 이유를 적는다.

**표는 색인으로 좋지만 칸마다 값을 치른다.** `573` 에서 `증상 → 어느 쪽 → 처방` 판별 표를 만들자 라우팅이 한눈에 들어왔는데, 같이 넣은 `무엇의 문제` 칸은 바로 아래 설명과 중복이라 걷어냈다(+14% → +12%). **칸을 늘리기 전에 그 칸이 본문 어딘가와 겹치는지 본다.**

**`kind` 는 문서의 주된 일을 따른다.** 레시피 절이 생겼다고 자동으로 `Recipe` 가 되지 않는다 — `576` 은 `# Schema`·`# Examples` 가 여전히 본체라 `Convention` 유지, `575`·`573` 은 절차가 본체가 돼서 `Recipe` 로, `570` 은 절차 절이 생겼어도 본체가 *"믿어도 되나"* 라는 논증이라 `Explainer` 로 남았다.

**`kind` 문서에서는 대체가 아니라 층이다.** 아래 `kind` 예외와 충돌하지 않는데, 이유는 모양이 같아서가 아니라 **다른 층을 맡기 때문**이다 — `# Schema`·`# Examples` 는 *무엇인가*(소비자용), 레시피는 *어떻게 쓰는가*, `# 기각` 은 *왜 안 했나*. `576` 이 그 셋을 같은 `#` 레벨에 평평하게 놓고 있었고, 흩어져 있던 세 절(`편입 기준`·`Guard`·`타입이 못 잡는 전제`)이 실은 **한 레시피의 절차·확인·함정**이었다. `kind` 는 그대로 둔다 — 문서의 주된 일이 규칙을 세우는 것이면 레시피 절이 생겨도 `Convention` 이다.

### 검증은 사용의 흔적이다

**정리 라운드로 `verified` 를 채우러 다니지 않는다.** 메모를 꺼내 쓰는 순간이 곧 검증이다 — 돌아가면 그때 찍고, 안 돌아가면 그때 고친다. 안 꺼내 쓴 메모는 검증되지 않은 채로 둔다. **"검증 안 됨"은 결함이 아니라 "아직 안 써봤다"는 사실이다.**

검사가 한 칸씩 무거워진 경로가 있다 — 형식 검사가 못 잡아서 출처 대조를 넣었고, 출처 대조가 못 잡아서 실행을 넣었고, 그러다 메모 하나를 검증하려고 테스트를 짜려 했다. **"못 잡은 게 있다"가 더 무거운 검사를 정당화하지는 않는다.** 잡히지 않은 것의 비용과 잡으려는 비용을 비교해야 하는데 한 번도 안 했다 — 541개에서 실제로 나온 결함은 8개다(`256`·`243`·`560`·`307`·`125`·`48`·`160`·`270`).

선을 넘은 지점은 **`verified` 가 문서의 속성이 된 순간**이다. `lint:links` 는 재실행되고 결과가 같다 — 기계 검사다. `verified` 는 한 번 한 일을 문서에 박제하고, 박제된 것은 낡는다. 낡지 않게 하려면 계속 재검증해야 하고 그게 메모를 프로그램처럼 유지보수하게 만든다. `stale_after` 를 채택하지 않은 게 그 신호였다 — "언제까지 유효한가"에 답할 수 없다면 애초에 박제가 맞는 형태가 아니다.

**대상마다 상한이 다르다.**

| 대상 | 상한 | 누가 |
|---|---|---|
| 링크 (`bookmarks`) | `lint:links` 재실행 | 기계. **`verified` 를 쓰지 않는다** |
| 주장 (`note`·`Explainer`) | 출처 대조 | 에이전트도 가능. 돌릴 게 없으니 여기가 상한이다 |
| 레시피 (코드 있는 것) | 실행 관찰 | **저자만.** GUI·계정이 필요한 건 에이전트가 못 한다 |

에이전트는 출처 대조까지다. 못 채우는 칸을 지어내면 그게 새 형식 닻이다.

### LLM이 정리할 때

**문체는 기준이 아니다.** LLM이 산문을 쓰는 것도, 압축하는 것도 괜찮다. 번역문이 압축된 한국어가 되는 건 개선이다.

경계는 문체가 아니라 **사실의 출처**다.

| 출처 | LLM이 해도 되나 |
|---|---|
| 저자가 준 사실 → 형태로 배치 | **된다.** 576을 OKF 형태로 옮긴 게 이 경우 |
| git·HTTP·본문 구조 → 전사 | **된다.** 재실행으로 검증되므로 (`generated.by` 백필, 링크 판정, 형태 검사) |
| 원천 없이 LLM이 판단 | **안 된다.** 모르면 지어내지 말고 질문으로 남긴다 |

`resource`·`sources`·`kind`·`stale_after` 같은 값은 코퍼스에 없다 — 저자만 안다. 검색해서 안 나오면 "없음"이 아니라 **"물어봐야 함"**이다. 그럴듯하게 채우면 소비자가 구분할 방법이 없다.

- **저장소 안의 원천을 먼저 본다.** 572.md의 4구간(`send`/`in-flight`/`processing`/`resp`)은 지어낸 구분이 아니라 `public/iframe/request_abort_race_sim.html`에서 읽은 것이다. iframe·SVG·설정 파일에 이미 답이 있는 경우가 있다.
- **부풀린 문장은 잡히지만 눌러 담은 문장은 안 잡힌다** — 576.md는 린터 둘 다 조용했는데 과압축이었다. 기계 검사가 통과하는 것과 잘 쓰인 것은 다르다.
- **요청된 층에서만 일한다.** 형태 작업(쿡북 적용·압축·`type` 정정)에 검증을 섞지 않는다. 메모를 열면 출처부터 대조하는 관성이 붙는데 그건 검증 라운드의 습관이지 형태 작업의 일부가 아니다 — `576`에 쿡북을 적용하면서 `dayjs`·`/src/lib/date.ts`·`/rules/*` 존재 여부부터 확인한 게 그랬다. 본문만으로 완결되는 작업이었고, `resource`는 애초에 **저자만 아는 값**이라 내가 판정할 것도 아니었다. 뒤에 있는 건 **결함을 찾으면 쓸모 있어 보인다는 압력**이다 — `lint:voice`가 오염원이 된 것과 같은 기제다.

### 예외: `kind`가 있는 문서

`kind`가 붙은 문서는 **OKF concept이지 메모가 아니다.** 독자가 6개월 뒤의 내가 아니라 소비자(에이전트·검색·교환)라서, 위 압축 원칙과 아래 "하지 말 것"이 적용되지 않는다 — OKF §4.2는 반대로 구조적 마크다운(헤딩·표·`# Schema`·`# Examples`)을 권한다. 명세는 [docs/memo-spec.md](docs/memo-spec.md)의 "OKF concept" 절.

"원칙을 무시한다"가 아니라 **"이건 다른 장르다"라는 사실을 적는 것**이다.

### 예외: 에세이 (308·379·489)

`think` 컬렉션에서 돌아온 세 편은 **의견 에세이지 메모가 아니다.** 1,600~1,900자 산문에 서론→본론→결론이고 아래 "하지 말 것"과 정면으로 부딪히지만, 저자가 직접 쓴 글이고 주장 자체가 내용이라 압축하면 슬로건만 남는다 — 그래서 원문을 보존한다 (`generated: { by: human:cbcruk }`).

**늘어나면 안 되는 예외다.** 새 글이 이 형태로 나오면 메모가 아니라 에세이를 쓰고 있는 것이니, 예외를 늘리기 전에 그게 맞는지 먼저 묻는다.

### 하지 말 것

- 서론 → 본론 → 결론 구조
- 친절한 독자 배려 문장
- 불필요한 섹션 분리
- "이 글에서는...", "결론적으로..." 같은 블로그 투

### 할 것

- 핵심 먼저, 맥락은 최소한
- 코드 블록 + 짧은 설명
- 미래의 나를 위한 힌트
- 나중에 확장 가능한 씨앗

---

## 메모 명세

**명세는 [docs/memo-spec.md](docs/memo-spec.md)에 있다** — 필드 목록, `type`·`status` 기준, 태그·각주 규칙, 검사 체계, 채택하지 않은 필드와 근거.

여기서는 쓸 때 바로 필요한 것만 둔다.

```yaml
---
type: note              # 필수, 'bookmarks' | 'snippet' | 'note' (형태)
tags: ['tag1', 'tag2']  # 필수, 빈 배열 [] 허용 (주제)
status: draft           # 필수, 'draft' | 'archive' | 'release'
ctime: YYYY-MM-DD       # 필수
mtime: YYYY-MM-DD       # 필수
---
```

- 선택 필드(`title`·`description`·`embed`·`voice`·`parent`·`relation`)는 명세 참고
- **`type`은 형태, `tags`는 주제.** 둘을 섞지 않는다
- **`bookmarks`는 `draft`를 쓰지 않는다** — 링크는 붙여넣은 순간이 최종형이다
- 어긋나면 빌드가 잡는다: `pnpm lint:memo` (형태·각주·명세↔스키마)

### Alerts (GitHub 스타일)

중요 정보 강조 시 사용. 남용 금지 (메모당 1-2개 이하).

```markdown
> [!NOTE]
> 참고할 만한 정보

> [!TIP]
> 유용한 팁

> [!IMPORTANT]
> 핵심 정보

> [!WARNING]
> 주의 필요

> [!CAUTION]
> 위험/부정적 결과 경고
```

- 연속 배치 금지
- 다른 요소 안에 중첩 금지

---

## 메모 유형 및 템플릿

### Type A: 링크 모음 (bookmarks)

```markdown
---
type: bookmarks
tags: ['keyword']
status: archive
ctime: YYYY-MM-DD
mtime: YYYY-MM-DD
---

- [링크 제목](URL)[^N-1]
- [링크 제목](URL)[^N-2]

---

[^N-1]: 간단한 설명
[^N-2]: 간단한 설명
```

### Type B: 코드 스니펫

```markdown
---
type: snippet
tags: ['typescript', 'pattern']
status: release
ctime: YYYY-MM-DD
mtime: YYYY-MM-DD
---

한두 줄 설명

\`\`\`tsx
// 코드
\`\`\`

---

- 참고: [링크](URL)
```

### Type C: 기술 문서

```markdown
---
type: note
tags: ['react', 'architecture']
status: draft
ctime: YYYY-MM-DD
mtime: YYYY-MM-DD
title: 선택사항
description: 선택사항
---

# 제목

소개 문단

## 섹션 1

내용

## 섹션 2

내용

---

## 참고 자료

- [링크](URL)
```

### Type D: 비교/분석

```markdown
---
type: note
tags: ['library', 'comparison']
status: release
ctime: YYYY-MM-DD
mtime: YYYY-MM-DD
---

## 옵션 A

설명, 장단점

## 옵션 B

설명, 장단점

## 결론

추천 사항
```

### Type E: 아이디어/실험

```markdown
---
type: note
tags: ['idea', 'keyword']
status: draft
ctime: YYYY-MM-DD
mtime: YYYY-MM-DD
---

## 아이디어

배경, 동기

## 접근 방식

시도할 것들

## 메모

관련 리소스, 참고사항
```

### Type F: 문제 해결 (디버그)

버그/이슈 추적은 **객관적 사실 위주**로. 현상 → 원인 → 해결 순서. 추적 과정의 서사·개인 감상은 덜어내고, 일반화되는 교훈만 1~2줄 남긴다.

```markdown
---
type: note
tags: ['debug', 'keyword']
status: release
ctime: YYYY-MM-DD
mtime: YYYY-MM-DD
---

한 줄 결론 (문제 = 원인)

## 현상

객관적으로 관찰된 사실 (재현 조건, 증상, 비대칭 단서)

## 원인

검증된 근본 원인 (+ 근거)

## 해결

적용한 방어/수정 (+ 주의사항)

## 교훈

일반화되는 원칙만 1~2줄 (개인 감상 X)

## 참고

- [링크](URL)
```

원칙:

- "추적기", "회고" 같은 서사 프레임 지양 — 6개월 뒤 "그래서 뭐였지"에 즉답되는 형태
- 일반화 사실(예: "URL 인코딩은 멱등이 아니다")은 보존, 그 순간의 감상(예: "어디서 멈출지 판단이 중요했다")은 제거
- 레퍼런스 예시: `539.md`

### Type G: 레시피 (쿡북)

`언제`가 색인이다. `절차`는 따라 하면 돌아가야 하고, `확인`이 없으면 맞았는지 알 수 없다.

```markdown
---
type: note
kind: Recipe
tags: ['keyword']
status: release
ctime: YYYY-MM-DD
mtime: YYYY-MM-DD
---

한 줄 목표 (이 레시피가 무엇을 만드는가)

**언제**: 이 레시피를 꺼내는 상황

**재료**: 전제·의존 (없으면 생략)

**절차**

1. 실행 단계
2. 실행 단계

\`\`\`ts
// 붙여넣어 돌아가는 코드
\`\`\`

**확인**: 됐는지 아는 방법

**함정**: 실제로 밟은 것만
```

---

## 워크플로우

### 새 메모 추가

1. **중복 확인**: 관련 태그/키워드로 기존 메모 검색
2. **파일 생성**: 다음 숫자 ID로 `.md` 생성
3. **유형 선택**: 위 템플릿 중 적절한 것 선택
4. **type 지정**: `bookmarks` / `snippet` / `note` (형태)
5. **태그 지정**: kebab-case로 관련 태그 추가 (주제)
6. **status**: 작성 중이면 `draft`, 저장만 해두면 `archive`, 완성되면 `release`

### 메모 수정

1. `mtime` 업데이트
2. 필요시 `status` 변경 (draft → release, archive → release)

---

## 변경 이력

- **`570` 에 쿡북 적용 (일곱째) — 처방이 한 줄인 줄 알았는데 펴니 셋이었다.** *"입력창은 빠른 쪽을, 무거운 목록은 늦은 쪽을 쓴다"* 가 **두 단계를 한 문장에** 붙이고 있었고, 세 번째(`isStale` 을 화면에 알린다)는 저 아래 *"결국 따라잡는다"* 불릿 **끝에 묻혀** 있었다 — 보장을 설명하는 문장 안에 처방이 섞여 있던 것이다. `확인` 은 둘로 갈렸다: *"입력창이 즉시 반응하는가"* 는 내 배선을 확인하고, *"손을 멈추면 따라잡는가"* 는 React 의 보장을 확인한다 — 앞엣것만 검산이다. `kind: Explainer` 유지: 절차 절이 생겼어도 본체는 여전히 *"믿어도 되나"* 라는 논증이다(`575`·`573` 은 절차가 본체가 돼서 `Recipe` 로 갔다). 1,564 → 1,693자(+8%)
- **`572` 는 쿡북을 적용하지 않았다 — 처음 나온 "아니오".** 앞의 다섯(`578`·`576`·`575`·`574`·`573`)은 전부 처방이 있었는데 `572` 는 **한 줄도 없다.** 있는 건 "이건 이렇게 동작한다"(취소는 클라이언트에서만, 전송이 끝났으면 서버는 받았다)뿐이다. 여기서 역검사가 나왔다 — **`확인` 칸이 안 채워지면 레시피가 아니다.** `언제` 도 되고 `절차` 1번도 되는데 2번이 없다. `573`·`574` 의 `확인` 은 *내 수정이 먹혔는가* 를 봤지만 `572` 에서 만들 수 있는 건 *설명이 맞는가* 를 시뮬레이터로 보는 것뿐이고 그건 검산이 아니라 학습이다. **"안 맞는 걸 만나면 이유를 적고 넘어간다"의 첫 적용이다** — `305` 를 부풀리려 했던 것과 반대 방향
- **`573` 에 쿡북 적용 (다섯째) — 처음으로 크기가 다시 뛰었고, 원인이 내가 만든 표였다.** 원문은 불릿 두 개에 **증상·분류·처방·기제·함정 다섯 가지를 각각 우겨넣고** 있었다 — 앞쪽/뒤쪽을 가르는 판별이 처방과 한 문장에 붙어 있어서 "내 경우가 어느 쪽인지" 고르는 단계가 안 보였다. `증상 → 어느 쪽 → 처방` 표로 떼어내니 라우팅이 드러났다. 다만 같이 넣은 `무엇의 문제` 칸이 바로 아래 `왜 갈리나` 와 중복이라 걷어냈다(+14% → +12%) — **표는 색인으로 좋지만 칸마다 값을 치른다.** `확인` 은 `574` 와 같은 경로로 나왔다: 데모의 `title` 이 이미 "응답 시각과 최소 노출 시간을 조절하며 관찰"이라고 적고 있었다. `kind: Explainer` → `Recipe`
- **`574` 에 쿡북 적용 (네 번째 표본) — 갈림길이 결론 자리에 있었다.** *"이 값이 재계산되면 틀린 동작인가 느린 동작인가. 틀림 → state/ref, 느림 → useMemo"* 는 **이 메모를 꺼내는 이유 자체**인데 본문 맨 아래 있었다. 색인이 없는 게 아니라 **끝에 있어서 색인 노릇을 못 했다** — 위에서 아래로 다 읽어야 도달한다. 맨 위 `언제` 로 올리니 `578` 로 가는 링크도 갈림길의 한쪽 가지로 제자리를 찾았다. `확인` 은 본문에서 나왔다: 캐시 폐기 이벤트에 "개발 중 파일을 고칠 때"가 이미 적혀 있어서 **저장해 보고 색이 그대로면 된 것**이다. `kind: Convention` 유지 — 주된 일이 "어디에 두는가"라는 규칙이다. 1,876 → 1,966자(+5%)
- **`575` 에 쿡북 적용 (세 번째 표본) — 주장이 실은 검산이었다.** *"있을 수 없는 조합은 아예 적을 수가 없다"* 는 문장 그대로 확인 방법인데, **"직접 만들면 X / 선언적으로 쓰면 Y" 대조 구조** 안에 있어서 결론처럼 읽혔다. 대조는 *왜 나은지* 를 설득하고 절차는 *무엇을 하는지* 를 말한다 — 칸을 만드니 그 문장이 `확인` 으로 옮겨갔다. 괄호에 묻혀 있던 `573` 링크도 `함정` 으로 올라오면서 "이 축으로는 안 고쳐진다"가 분명해졌다. `kind` 는 `Explainer` → `Recipe`: 절차가 본체가 됐다(`576` 은 반대로 `Convention` 유지). 833 → 896자(+8%)로 세 표본 중 가장 덜 늘었다
- **"요청된 층에서만 일한다"를 규칙으로 추가했다.** `576` 에 쿡북을 적용해달라는 요청에 `dayjs` 설치 여부·`/src/lib/date.ts`·`/rules/*` 존재부터 확인했다 — 본문만으로 완결되는 작업이었고 `resource` 는 저자만 아는 값이라 판정할 것도 아니었다. 트리거는 `resource:` 를 리포 상대 경로로 읽은 것과 직전 검증 작업(`270`·`352`)의 관성이고, 더 깊은 원인은 **결함을 찾으면 쓸모 있어 보인다는 압력**이다 — `lint:voice` 가 오염원이 된 것과 같은 기제다. 게다가 30분 전에 커밋한 "검증은 사용의 흔적이다"를 바로 어긴 것이었다
- **`576` 에 쿡북을 적용 — 두 번째 표본에서 나온 것: `kind` 문서에서 쿡북은 대체가 아니라 층이다.** `# Schema`·`# Examples`(무엇인가) / 레시피(어떻게 쓰는가) / `# 기각`(왜 안 했나)이 같은 `#` 레벨에 평평하게 있어서 어느 걸 먼저 보는지 알 수 없었다. 흩어져 있던 `편입 기준`·`Guard`·`타입이 못 잡는 전제` 세 절이 **한 레시피의 절차·확인·함정**이었다 → `# 표기 추가하기` 하나로. `kind: Convention` 은 그대로 뒀다 — 문서의 주된 일이 규칙을 세우는 것이면 레시피 절이 생겨도 `Convention` 이다. 크기 1,279 → 1,389자(+9%)로 `578`(+16%)보다 덜 늘었다: 새로 쓴 게 아니라 있던 것을 묶어서다
- **쿡북의 위상을 "최종 모델"이 아니라 "기본값"으로 못박았다.** 하루 써보니 뼈대가 `578` 에서 결함 두 개를 드러냈다 — **빈 칸이 눈에 보이는 게 좋은 형식의 정의**고, `type`·`lint:voice`·OKF 필드는 "무엇이 빠졌는지"를 물은 적이 없다. 그런데 같은 날 반례도 나왔다: breadcrumb 를 결함으로 판정하고 부풀리려 한 것이 **"최종 모델"을 전제했을 때의 실패 양상**이다. 안 맞는 걸 만나면 이유를 적고 넘어가지, 대상을 고치지 않는다. **갈림길은 `type`·`kind` 가 아니라 "나중에 실행할 일이 있는가"** — `bookmarks` 290개·breadcrumb·`Explainer` 는 안 맞는 게 아니라 실행할 일이 없는 쪽이다. 크기도 명시했다: `578` 이 1,177 → 1,360자로 90퍼센타일(1,105)을 넘었는데 코퍼스 중앙값은 467자다. 맨 위 기본형과 **두 기본형이 공존하고 크기로 서로를 판정하지 않는다.** 표본이 `578` 하나라는 것도 함께 적었다
- **검증을 정리 라운드에서 사용 시점으로 옮겼다 (`### 검증은 사용의 흔적이다`).** 메모가 미니 프로그램이 되어가고 있었다 — 형식 검사 → 출처 대조 → 실행으로 검사가 한 칸씩 무거워졌고, 급기야 메모 하나(`578`)를 검증하려고 vitest 테스트를 짤 뻔했다. 숫자가 문제를 드러낸다: `verified` 65개 중 **44개(68%)가 코드가 없다** — 돌릴 게 애초에 없는 문서에 실행 어휘를 씌우고 있었다. `bookmarks` 10개에서 `verified` 를 걷어낸다 — 링크 생존은 `lint:links` 가 매번 재실행으로 판정하니 박제할 이유가 없다. `note` 46개는 남긴다: 출처 대조는 재실행으로 확인되지 않고, `256` 이 출처와 반대라는 걸 잡아낸 게 그 작업이다. **잃는 것도 있다** — bookmarks 의 `verified` 는 "200 OK" 가 아니라 "문서가 아직 거기 있다" 였고(`125`·`48` 은 소멸한 문서였다) `lint:links` 는 그걸 못 본다. 링크는 클릭하는 순간이 곧 사용이니 그때 고치는 것으로 대신한다
- **`578` 을 쿡북으로 옮기고, 그러다 린터가 Type G 를 못 읽는 걸 발견했다.** 옮기면서 나온 것: 본문이 *"인라인으로 넘기면 한 번도 재활용되지 않는다"* 까지만 말하고 **처방이 없었다** — `const items = useMemo(() => data.map(transform), [data])` 는 저장소 안 시뮬레이터 `useMemo_cache_behavior.html` 시나리오 2에 이미 있었다. 각주에 방증으로만 있던 "개발 모드는 계산 함수를 두 번 부른다"도 `확인` 칸에서 제 몫을 하게 됐다 — `console.count` 로 세면 렌더당 2씩 뛰는 게 정상이라 모르면 오독한다. **린터 쪽**: 레시피는 "짧은 코드 조각 + 번호 절차" 모양인데 번호 절차가 산문으로 안 세져서 `classify()` 가 `snippet` 으로 본다. `steps >= 3` 만으로 열었더니 코드 13~29줄짜리 진짜 `snippet` 셋(`385`·`534`·`563`)이 딸려 들어와서, **저자가 선언한 `kind: Recipe` 를 키로** 좁혔다 — 코드만 있는 `Recipe`(`243`)는 절차가 없으니 그대로 `snippet` 이다
- **`kind: Recipe` 9개를 따라가 봤다 — 3개가 걸렸고, 2개는 내 오판이었다.** 걸린 것: `270` **동작 안 함**(`createRoot('#confirm-root')` — react.dev 는 `domNode: A DOM element` 라고 못박고 셀렉터 문자열은 "Target container is not a DOM element" 를 던진다 → 수정), `352` **어디에 입력하는지 누락**(Chromium `interstitial_large.js` 로 확인 — 경고 페이지의 keypress 핸들러가 `BYPASS_SEQUENCE` 와 대조해 `CMD_PROCEED` 를 보낸다. 그래서 입력창이 없다 → 보완), `311` **오분류**(절차가 없다 → `Explainer`). **오판: `305`·`97` 을 "목차"라고 불렀다.** 저자가 순서를 기억하려고 남긴 breadcrumb 인데 형태만 보고 결함으로 단정했고, GCP 절차를 채워 넣겠다고까지 했다 — 그게 원래 가치를 없애는 쪽이었다. **형식으로 결함을 못 잡는다고 했으면서, 형식으로 결함을 단정한 셈이다.** 걸린 3개는 전부 `lint:memo`·압축 원칙·OKF 필드를 통과한다. 더 아픈 건 `270` 에 `verified: { by: claude/opus-5 }` 가 붙어 있었다는 것이다 — 8월 19일 검증이 **링크는 대조했지만 실행은 안 했다.** 링크가 살아 있는 것과 코드가 도는 것은 다르다
- **쿡북을 검산으로 도입했다 (`### 쿡북` + `Type G`).** 지금까지 만든 검사는 전부 형식이었다 — `lint:memo`·압축 원칙·철거된 `lint:voice`·OKF 출처 필드. `305.md`가 그 넷을 다 통과하는데 "1. 서비스 계정 설정 2. 프로젝트 생성 …" 목차 7줄이라 따라 할 수가 없다. **형식으로는 못 잡고 따라 해봐야 나온다** — "출처를 실제로 가져와 대조해야 검증이다"와 같은 계열이고, 레시피에서 그 열림은 실행이다. 뼈대에서 무게를 지는 건 `언제`(상황 색인 — 없으면 목록이 아니라 문서가 된다)와 `확인`(맞았는지 아는 방법)이다. **실행 가능 ≠ 코드**: 판별 절차·점검 목록·질문 세트도 실행된다. 크기 문제는 **메모 하나 = 레시피 하나**로 푼다 — 여러 개면 라우터 레시피를 두고 `parent`로 묶는다
- **`think` 컬렉션을 `memo`로 되돌렸다 (`308`·`379`·`489`).** 별도 스키마·헬퍼·라우트 2개·Nav 항목·`Category` 분기를 유지하면서 정작 검색·RSS·`llms-full.txt`·`/tagged`·유사도에는 한 번도 안 잡히고 있었다 — **격리 비용만 있고 도달은 없었다.** `git log --follow`를 따라가 보니 셋 다 원래 메모였다: `memo/308`·`memo/379`·`memo/489`가 2025-04-20에 `think/`로 옮겨졌고 그 뒤 `1`·`2`·`3`으로 재번호됐다. 세 ID가 비어 있어서 그대로 복귀시켰다 — 새 번호(`579`~`581`)를 주면 원래 URL이 영영 빈 채로 남는다. 이번 통합에서 걸린 것들: `summary` 필드는 유일 소비자였던 목록 라우트와 함께 제거(`description`이 대신한다), 한국어 공백 태그 20개를 kebab-case 6개로 재매핑(memo 고유 태그 477개 중 한국어는 3개뿐이라 그대로 넣으면 `/tagged`와 IDF가 흔들린다). `essay` 태그는 달지 않았다 — `type`(형태)을 `tags`(주제)에 섞는 것이고, `bookmarks` 태그를 `type`으로 옮기며 이미 되돌린 실수다. 장르 예외는 산문으로만 적는다. 셋 다 `draft`로 두었으므로 `/memo/[id]`가 빌드되지 않는다 → `vercel.json`의 `/think/*` 리다이렉트는 `permanent: false`다. `release`로 올릴 때 함께 `true`로 바꾼다
- **OKF 출처·검증 필드를 도입했다 (`kind`·`generated`·`verified`).** `type`(형태)은 저장소가 유지하고 OKF 종류는 `kind`로 나란히 뒀다 — 이름을 뺏으면 `/type` 라우팅·유사도·`lint:memo`가 전부 따라 움직여야 한다. `resource`·`sources`·`stale_after`는 파일에만 두고 스키마에 선언하지 않는다: `entry.data`에는 없지만 `/memo/[id].md`와 `/llms-full.txt`가 원본 파일을 읽으므로 소비자에게는 도달한다 — **스키마 선언은 *사이트가 쓸 수 있는가*를 정하지 *소비자가 볼 수 있는가*를 정하지 않는다.** 공개 293개 중 71개 처리(`note` 전체 + 에이전트가 다시 쓴 19개). 명세는 [docs/memo-spec.md](docs/memo-spec.md) "OKF concept" 절
- **검증은 출처를 실제로 가져와 대조해야 검증이다.** 텍스트를 다시 읽는 건 닫힌 루프다. 형식 검사는 전부 통과했는데 URL을 열어보고서야 나온 것들 — `256` 주장이 출처와 반대(→ 삭제), `243` 동작하지 않는 레시피(`document.fonts.add()` 누락), `560` `facebook/react` → `react/react` 조직 개명, `307` `main` 브랜치 라인 링크 드리프트(→ SHA 고정), `125`·`48` 소멸한 문서, `160` 서비스 종료
- **형식 닻 5건 제거 + `generated.by` 오귀속 14건 정정.** `ui_exclusive` 등 Lean 정리 이름을 인용하고 있었으나 도입 커밋이 에이전트 co-author였고 `.lean` 파일도 웹 검색 결과도 없었다. 전사 규칙이 git `%an`만 봐서 `Co-Authored-By: Claude`인 사람-저자 커밋을 사람 작성으로 기록한 것도 함께 고쳤다
- **"왜 쓰는지"가 통째로 빠진 메모가 있다.** 초등학생 눈높이로 다시 써보려니 드러났다 — `570`·`578` 둘 다 기제로 바로 진입하고 용도를 한 번도 말하지 않았다. 한 메모에 두 주제가 있으면 **인바운드 링크가 절반만 겨냥한다**는 것도 그때 보였다(`575` → `570`) → 분리
- **`256.md` 삭제 — 검증에서 주장이 출처와 반대인 게 드러났다.** *"모듈 페더레이션은 빌드 레벨"*이라고 썼는데 공식 사이트가 스스로를 정반대로 설명한다: *"Teams can consume components **at runtime** instead of as part of their build pipeline"*. 링크는 살아 있고 형식 검사도 통과했다 — **출처를 실제로 가져와 대조해야만 나오는 종류의 오류**다
- 압축 원칙 4개 추가 — 기호가 암호가 되는 것(575·576·572), 걷어낸 프레임의 잔여물(`combobox`·`alt`·`title`), 확인 불가한 권위 표시(형식 닻 5건 제거), 저장소 안의 원천 먼저 보기(시뮬레이터에서 구간을 읽음)
- **문체 검사를 철거했다 (`lint:voice`·`voice: author`).** 문체는 기준이 아니라고 정리했다 — LLM이 산문을 쓰는 것 자체는 문제가 아니고, 번역문이 압축된 한국어가 되는 건 개선이다. 실제로 막혔던 건 문체가 아니라 **사실의 출처**였다: 576에서 `resource`·`sources`·`kind`·`stale_after`는 코퍼스에 없고 저자만 아는 값이라, 문체를 아무리 봐도 나오지 않았다. 경계를 "원천 없으면 질문"으로 옮기고 검출기(147줄)와 필드를 걷어냈다
- **`lint:voice`를 빌드 게이트에서 뺐다 — 검증이 오염원이었다.** 검출 29 → 0을 만든 커밋 중 21개가 에이전트 재작성이었다(`a0feaf6`·`8a944e9`·`f534e87`·`4667b77`·`d74ed59`·`d31619e`). 게이트가 검출을 "지금 고쳐야 할 것"으로 만들고, 가장 빠른 처방이 재작성이라 번역문이 에이전트 문장으로 교체됐다. 목적("내 언어로 줄인다")은 달성되지 않고 수치만 0이 됐다. 오염 없이 끝난 정리는 처방이 "삭제만"이었던 `db5f660`(5개) 하나뿐이다. 형식 검사(`lint:memo`)는 처방이 기계적이라 게이트로 남긴다
- 명세를 `docs/memo-spec.md`로 분리. CLAUDE.md는 판단(작성 원칙·압축·템플릿)만 남기고 명세를 가리킨다 (389 → 305줄). 같은 규칙이 스키마·린터·문서·스킬 네 곳에 흩어져 있어서, 최소한 **명세와 스키마가 어긋나는 건 `lint:memo`가 잡는다** — 실제로 `embed`가 스키마에만 있고 문서에 없었다
- 각주 짝 정리: 7개 파일 (정의 없는 참조 3 / 참조 없는 정의 4). 정의가 남아 있던 3개는 번호 순서가 위치를 알려줘서 마커를 복원했고, 붙을 곳이 없던 1개는 삭제. `lint:memo`에 규칙 추가
- 압축 원칙 추가: 존댓말을 "남의 문장" 신호로 사용. 검출 29개 → 0개 (공개 23개 → 0개). `lint:voice`를 빌드에 연결(`release`만 실패)하고, 저자 본인 문체는 `voice: author`로 표시
  - 산문 삭제/압축: 268·272·274·46·173·21·56·243·307·149·329·247·271·340·23
  - 각주만 문제였던 것: 83·19·24·221·312 (링크 제목에 이미 있는 내용을 늘려 쓴 경우. 각주에 박혀 있던 기술 링크는 살렸다)
  - 비공개도 정리: 123·125·171(각주), 182·256·267(산문). 125에서 중복 링크 1개 제거
  - `309.md`는 본인 작업 기록인데 존댓말이라 `voice: author`를 달아 예외로 표시
  - 반대 방향도 있었다: `576.md`는 본인 문장인데 과압축·자기 반복이라 산문이 코드를 다시 말하고 있었다 (566자 → 476자, iframe 제거로 `.mdx` → `.md`)
  - 걷어내니 형태가 드러나 `type`이 바뀐 것: 46·269·23·247·271·182·256(→`bookmarks`), 56·340(→`snippet`), 329(→`note`)
- 태그 정규화: snake_case → kebab-case 변환 (23개 파일)
- 빈 태그 메모: 28개 파일에 태그 적용
- 삭제: 427.md (내용 없음)
- `archive` 상태 도입: `bookmarks`의 `draft` 231개를 이동. `draft`가 "작성 중"과 "공개 안 함" 두 뜻으로 쓰이던 것을 분리 (작성 중 큐 268 → 37)
- `type` 필드 도입 + 542개 파일 백필 (bookmarks 274 / snippet 188 / note 80). `bookmarks` 태그는 `type`으로 이동, 유사도 계산의 `STRUCTURAL_TAGS` 예외 제거
